import { icons } from 'lucide-react';

type Props ={
    name: String,
    color?: String,
    size?: String
}

const Icon = ({name, color, size}: Props) => {
  // const LucideIcon = icons[name];

  return <></>
};

export default Icon;